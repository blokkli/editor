query pbGetPreviewGrantUrl(
  $entityType: EntityType!
  $entityUuid: String!
  $langcode: String!
) {
  getParagraphsEditState(
    entityType: $entityType
    entityUuid: $entityUuid
    langcode: $langcode
  ) {
    previewUrl
  }
}
